# `cdtree` コードレビュー＆新機能追加提案

本ドキュメントでは、RustによるCLIディレクトリ移動ツール `cdtree` のコードベースのレビューと、今後有用になりそうな新機能の提案を行います。

## 1. コードレビュー (Code Review)

全体的に `ratatui` や `crossterm` などの定番クレートを利用し、綺麗にレイヤーが分かれて実装されています。ここでは、さらに品質を高めるためのいくつかのアドバイスと気になった点を挙げます。

### 1.1 `FileNode` とディレクトリ構造のロード
- **遅延読み込みの実装**:
  `src/app.rs` 内の `FileNode::load_children` メソッドは、ディレクトリが展開されたタイミングで中身を読み込む遅延読み込みが実装されており、パフォーマンス上非常に優れています。
- **エラーハンドリング**:
  `fs::read_dir` で権限がないディレクトリを開こうとした場合などに `Err` が返る可能性がありますが、`load_children` で適切に拾っており、展開できなくするフォールバック処理 (`node.expanded = false`) がある点は頑健です。ただし、ユーザーになぜ展開できなかったのか（「Permission denied」など）をUI上で通知する仕組みがあると、より親切になります。

### 1.2 UI・描画処理 (`src/ui.rs`)
- **レンダリングの効率化**:
  `TreeWidget::render` 内で毎フレーム `get_visible_nodes` を呼び出し、ベクターの再確保とプレフィックス文字列の生成が行われています。もしディレクトリの数が膨大になった場合、キー入力ごとに計算コストがかかる可能性があります。描画用の可視ノードリストは `App` の状態が更新された（展開/折りたたみ、移動など）タイミングのみキャッシュするように修正すると、さらにパフォーマンスが向上します。

### 1.3 シェル連携 (`src/shell.rs`)
- **`has_shell_integration` の堅牢性**:
  現在の実装では、`.zshrc` や `.bashrc` の中に `source ".../launcher/bash/cdtree"` という文字列が**含まれているか**だけで判定しています。もし行がコメントアウトされていても `true` を返してしまう可能性があります。正規表現などで「行頭から有効な source コマンドとして記述されているか」を判定するとより正確になります。
- **XDG Base Directory 仕様への準拠**:
  設定ファイルやランチャースクリプトの保存先が `~/.config/cdtree` とハードコードされていますが、`std::env::var("XDG_CONFIG_HOME")` を優先的に利用することで、Linux等の環境でより行儀のよい動作となります。(`dirs` クレートを使用しているため、`dirs::config_dir()` を活用するのがベストプラクティスです)

### 1.4 設定・状態の保存 (`src/config.rs`)
- **設定ファイルのパス**:
  ここでも `HOME/.config/cdtree/config.json` がハードコードされています。`dirs::config_dir()` に置き換えるべきです。
- **シリアライズ/デシリアライズ**:
  `Config::load()` で `serde_json::from_str` が失敗した場合、黙って `Config::default()` にフォールバックしていますが、JSONが壊れている場合はユーザーに警告を出すか、バックアップを取ってからリセットする方が安全です。

---

## 2. 新機能追加提案 (Feature Proposals)

現状でもシンプルで使いやすいツールですが、以下の機能を追加することでさらに強力なCLIナビゲーションツールになると考えられます。

### 2.1 フィルタリング・検索機能 (Fuzzy Finder)
現在、ディレクトリを移動して探す機能はありますが、深い階層にあるディレクトリを名前で一発検索する機能がありません。
- **提案**: `/` キーを押下すると画面下部に検索入力エリアがポップアップし、入力した文字列で現在のツリー以下（または非同期でディレクトリを走査して）をファジー検索・フィルタリング表示する機能。

### 2.2 ファイルのプレビュー機能
現在はディレクトリの移動が主目的でファイルは無視（または非アクティブ）されていますが、設定ファイルなどを探しに行く際に中身を確認したい場合があります。
- **提案**: `f` キーでファイルを表示するモードにした際、ファイルにフォーカスが当たっている場合に、画面の右半分（または横分割）でファイルの中身（テキスト）をプレビューするペインを表示する機能。

### 2.3 ブックマーク・お気に入りディレクトリ機能
よく行くディレクトリ（例: プロジェクト群のルートなど）へは、ホームから辿るのではなく、一発でジャンプできると便利です。
- **提案**:
  - `m` キーで現在のディレクトリをブックマーク（マーク）に登録。
  - `'` (クォート) キーや `b` キーなどでブックマーク一覧のポップアップを開き、瞬時にジャンプできる機能。

### 2.4 マウスサポートの拡張
現在 `EnableMouseCapture` は有効になっていますが、UI側でマウスのスクロールやクリックが十分に活用されていません。
- **提案**:
  - ディレクトリをクリックで展開・折りたたみ。
  - スクロールホイールでリストを上下にスクロールする機能の実装。

### 2.5 カスタムコマンド実行 (Actions)
ディレクトリを選択した状態で、単に `cd` するだけでなく、他のコマンドを即座に実行できると便利です。
- **提案**:
  - 例: `e` キーを押すと環境変数 `$EDITOR` で現在のディレクトリを開く。
  - 例: `o` キーでシステムの標準ファイラー（macOSなら `open`、Windowsなら `explorer`、Linuxなら `xdg-open`）で現在のディレクトリを開く。

---

## 総評
コードは非常に整理されており、読みやすくメンテしやすい構成になっています。MVP（Minimum Viable Product）としての完成度は高いです。上記で挙げたパフォーマンス改善（キャッシュ化）やXDG対応を行いつつ、必要に応じて検索やプレビューなどの機能を追加していくことで、さらにユーザーベースを広げることができるでしょう。
